# 2026-04-25

## Session: Hermes integration verification + backup

### What was done
1. **lexx8 file-editing rule tightened** — added "use Write not Edit" guidance for files <2KB, full rewrites, or >50% changes; auto-fallback to Write after 2 consecutive Edit failures. Restarted openclaw, JSON validated. Backup at `/home/openclaw/.openclaw/agents/main/agent/agent.json.bak-20260425`.
2. **Backup created** — `/home/lexbot/backups/openclaw-hermes-20260425-0542/`:
   - `openclaw-config.tgz` (190M) — full `/home/openclaw/.openclaw/` minus sandbox/workspace/media/logs
   - `root-drafts-hermes.tgz` (16M) — `/root/drafts/` (hermes_worker.py, hermes-project.json, hermes-run-log.md)
   - `root-memory.tgz` (58K) — `/root/memory/` (mission-control-progress.md, daily memory files)
   - `cron-jobs.json` + `lexx8-agent.json` — quick-reference copies
3. **Hermes ↔ openclaw integration verified — already complete and healthy.**

### Hermes integration map
- **Two openclaw cron jobs** in `/home/openclaw/.openclaw/cron/jobs.json`, both targeting agent `lexx8`:
  - `hermes-operator-loop` (id `34ebf140…`) — every 30 min, `lastRunStatus: ok`, 114 runs logged
  - `hermes-status-pulse` (id `9c80a6c1…`) — every 10 min, `lastRunStatus: ok`, 319 runs logged
- **Worker scripts** at `/root/drafts/hermes_worker.py` (16K), `/root/drafts/hermes-project.json` (state)
- **Outputs** at `/root/memory/mission-control-progress.md`, `/root/drafts/hermes-run-log.md`
- Last run: 2026-04-25 05:38 UTC (4 min before this session ended), result OK
- Permissions intact (openclaw owns `/root/memory`, `/root` is 757)

### Issues observed (not blockers)
- **Phase regression in run log** — Hermes status pulse alternates between Phase 5 ("buyer/lender matching") and Phase 8 ("comm QC"). Project file says current_phase=8 but every other run reverts to 5. Likely a worker bug, not an integration gap.
- **Hardcoded date** — `hermes-operator-loop` payload writes to `/root/memory/2026-04-22.md` (hardcoded). Should be parameterized to today's date.
- **Other cron jobs broken (separate from Hermes):**
  - `inbox-wave`, `morning-brief`, `evening-brief` — "Outbound not configured for channel: telegram" (11+ consecutive errors on inbox-wave)
  - `inbox-cleanup-project` — 152 consecutive errors, "Delivering to Telegram requires target <chatId>"
- **OpenClaw update available** — 2026.3.24 → 2026.4.23 (one month behind)

### Note: `/home/lexbot/hermes-agent/`
The directory at `/home/lexbot/hermes-agent/` is the **upstream Nous Research Hermes Agent project** (separate Python codebase with its own gateway, MCP, docker). It is **NOT** the operating Hermes Operator — that runs as openclaw cron jobs against lexx8. The two share a name and nothing else.

### Recommended next actions
1. Fix Hermes phase oscillation in `/root/drafts/hermes_worker.py` (regresses Phase 8 → Phase 5 every other pulse)
2. Replace hardcoded `2026-04-22.md` in cron payload with today-relative date
3. Address Telegram outbound config to unblock inbox-wave / morning-brief / evening-brief delivery
4. Decide on openclaw 2026.4.23 update
