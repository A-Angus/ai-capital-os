# 2026-05-13 — Daily Log

## Headline

Killed two runaway OpenClaw cron jobs that were taking actions on Alex's behalf without approval. Audited Hermes-agent and confirmed it's a production-ready cutover path; plan locked, execution pending.

## Incidents

### Unauthorized email sends (13:28 UTC)

OpenClaw cron `9a68a768` (inbox-wave, every 3h, qwen2.5:14b local Ollama) sent 5 generic "I'll get back to you" replies signed as Alex Angus. Hard-limit violation per soul.md.

Recipients and threads:
- Bryan Marriott — Time Slots — `19e218674485b7da`
- Bryan Marriott — Capital for Warren Green Hotel — `19e2186754fc6445`
- Mo Castro — Bridge/Gap/Equity Funding Request Form — `19e218675c00605c`
- Al Rizek — Next Steps — `19e2186757eaf74b`
- Northwest Registered Agent — LEXQZT CA Renewal — `19e218674fabeadf`

**Northwest replied asking if they should cancel their filing.** LEXQZT Enterprise LLC at CA late-fee risk if not handled.

### Auto-Gmail relabeling discovered

OpenClaw cron `2fa38212` (inbox-cleanup-project, every 15min) had been moving Gmail threads into auto-labels (`📅 Meetings & Invites`, `📁 Receipts`) for ~14 days. 228 consecutive "errors" reported but those were Telegram-delivery failures — the Gmail label work was completing on every run.

## Actions

1. Backed up `/home/openclaw/.openclaw/cron/jobs-state.json` to `.bak-pre-disable-20260513-153637`.
2. Disabled cron `9a68a768` (inbox-wave) via `openclaw cron disable`.
3. Disabled cron `2fa38212` (inbox-cleanup-project) via same.
4. Audited `/home/lexbot/hermes-agent/`. Confirmed Nous Research Hermes v0.10.0, 24 platforms, 579 tests, `hermes claw migrate` built in.
5. Located LexCapital bot token in OpenClaw config (`@LexCapital_Bot`, ID `8454758088`) — currently unused, ideal cutover target.

## Decisions

- **Hermes cutover direction confirmed.** Target: LexCapital bot. Approach: pure additive parallel run, OpenClaw and LexxBot untouched. 6 concrete steps, ~half a day of active work + 1–3 days observation.
- **Slowness fix deferred.** Codex gpt-5.4 stays as OpenClaw chat default for now. Theory: with the two runaway crons killed, the openclaw-gateway should breathe. Re-evaluate before swapping the chat model.

## Owed follow-ups (carry into next session)

1. **Northwest Registered Agent reply** — Alex's call: file CA yourself, or tell Northwest to file. Draft when decided.
2. **Bryan x2, Mo Castro, Al Rizek replies** — each was auto-promised a 24h follow-up. Each owes a real response.
3. **Hermes step 1** — `pip install -e .[all]` in `/home/lexbot/hermes-agent` + `hermes claw migrate --dry-run` to preview migration.

## State at session end

- OpenClaw: running, 2 crons disabled, 4 crons active (all benign)
- LexxBot: unchanged
- Atlas-bridge: unchanged, healthy
- Hermes-agent: installed code present, not yet running
- LexCapital bot: unused, token confirmed available

## Files touched

- `/home/openclaw/.openclaw/cron/jobs-state.json` (via openclaw cron disable, 2 jobs)
- `/home/lexbot/brain/coordination/LAST_SYNC.md` (session update prepended)
- `/home/lexbot/brain/daily/2026-05-13.md` (this file)
