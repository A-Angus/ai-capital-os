# Ruff config for LEX AI OS
line-length = 100
target-version = "py312"

[lint]
select = ["E", "F", "W", "I"]
ignore = ["E501"]  # Line length handled by formatter

[format]
quote-style = "double"
indent-style = "space"
